public void union2Circle1Pipe(Circle circle1, Circle circle2, Pipe pipe) {
    if (circle1 == null || circle2 == null || pipe == null){
        throw new IllegalArgumentException("Не подходящие данные");
    }
    
}